name: 'Happi Labels'
description: 'Adds release labels to merged PRs and related PRs'
inputs:
  github-token:
    description: 'GitHub token'
    required: true
runs:
  using: 'node20'
  main: 'dist/index.js'
